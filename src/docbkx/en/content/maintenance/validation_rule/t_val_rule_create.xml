<?xml version='1.0' encoding='UTF-8'?>
<section version="5.0"
    xsi:schemaLocation="http://docbook.org/ns/docbook http://www.docbook.org/xml/5.0/xsd/docbook.xsd"
    xmlns="http://docbook.org/ns/docbook" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:xs="http://www.w3.org/2001/XMLSchema" xmlns:xl="http://www.w3.org/1999/xlink"
    xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xhtml="http://www.w3.org/1999/xhtml"
    xmlns:svg="http://www.w3.org/2000/svg" xmlns:mth="http://www.w3.org/1998/Math/MathML"
    xmlns:db="http://docbook.org/ns/docbook"
    xml:id="create_validation_rule">
    <title>Create a validation rule</title>
    <section>
        <title>Create a validation
            <?oxy_comment_start author="cecilia" timestamp="20170106T150214+0100" comment="Add URL to Mtn app doc."?>rule<?oxy_comment_end?></title>
        <para>This module provides management of validation rules. A validation rule is based on an
            expression which defines a relationship between a number of data elements. The
            expression has a left side, a right side and an operator which defines whether the
            former must be less than, equal to or greater than the latter. The expression forms a
            condition which should assert that certain logical criteria are met. For instance, a
            validation rule could assert that the total number of vaccines given to infants is less
            than or equal to the total number of infants.</para>
        <para>To add a validation rule, just follow these steps from the Data Quality -&gt;
            Validation Rule dialog:</para>
        <itemizedlist>
            <listitem>
                <para>Click on the <emphasis role="italic">Add new</emphasis> button</para>
            </listitem>
            <listitem>
                <para>Provide a descriptive <emphasis role="italic">Name</emphasis> for the
                    validation rule. The name must be unique among the validation rules.</para>
            </listitem>
            <listitem>
                <para>Provide a <emphasis role="italic">Description</emphasis> for the validation
                    rule.</para>
            </listitem>
            <listitem>
                <para>Select the <emphasis role="italic">Importance</emphasis> you wish to assign to
                    the validation rule: <emphasis role="italic">high</emphasis>, <emphasis
                        role="italic">medium</emphasis> or <emphasis role="italic"
                    >low</emphasis>.</para>
            </listitem>
            <listitem>
                <para>Select the <emphasis role="italic">Rule type</emphasis>. For validating
                    correct data, choose <emphasis role="italic">Validation</emphasis>. For
                    monitoring data according to a rule, choose <emphasis role="italic"
                        >Surveillance</emphasis> and follow the extra instructions below.</para>
            </listitem>
            <listitem>
                <para>Select the <emphasis role="italic">Period type</emphasis> for the data being
                    validated.</para>
            </listitem>
            <listitem>
                <para>Select an <emphasis role="italic">Operator</emphasis>. The operator options
                    are <emphasis role="italic">equal</emphasis> (==), <emphasis role="italic">not
                        equal</emphasis> (!=), <emphasis role="italic">greater than</emphasis>
                    (&gt;), <emphasis role="italic">greater than or equal</emphasis> (&gt;=),
                        <emphasis role="italic">less than</emphasis> (&lt;), <emphasis role="italic"
                        >less than or equal to</emphasis> (&gt;=), a <emphasis role="italic"
                        >compulsory pair</emphasis> or an <emphasis>exclusive
                    pair</emphasis>.</para>
                <para>The compulsory pair operator allows you to require that data values must be
                    entered for a form for both left and right sides of the expression, or for
                    neither side. In other words, you can require that if one field in a form is
                    filled, then one or more other fields must also be filled.</para>
                <para>The exclusive pair operator allows you to assert that if any value exist on
                    the left side then there should be no values on the right side (or vice versa).
                    Stated another way, this means that data elements which compose the rule on
                    either side should be mutually exclusive from each other, for a given time
                    period / organisation unit /attribute option combo. </para>
            </listitem>
            <listitem>
                <para>Define the <emphasis role="italic">left side</emphasis> and <emphasis
                        role="italic">right side</emphasis> of the validation rule expression.
                    First, provide a description for the expression. Second, build the expression
                    with the expression builder. The expression is mathematical and contain data
                    elements as well as integers and mathematical operators. Data elements can be
                    included by double-clicking one in the available data elements list to the
                    right. Alternatively one can select a data element and click the insert button.
                    Mathematical operators can be included by clicking the corresponding button
                    under the expression builder area.</para>
                <para>There are three states which can be set to alter how the validation rule will
                    be evaluated if there is any missing data. </para>
                <itemizedlist>
                    <listitem>
                        <para>Skip if any value is missing: The rule will be skipped if any of the
                            values which compose the expression are missing. This is the default
                            option. This option should always be selected if you are using the
                            &quot;Exclusive&quot; or &quot;Compulsory&quot; pair operator.</para>
                    </listitem>
                    <listitem>
                        <para>Skip if all values are missing: The rule will be skipped only if all
                            of the operands which compose it are missing. </para>
                    </listitem>
                    <listitem>
                        <para>Never skip: The rule will never be skipped in case of missing data,
                            and all missing operands will be treated effectively as a zero.</para>
                    </listitem>
                </itemizedlist>
            </listitem>
        </itemizedlist>
        <para>Save each expression by clicking <emphasis role="italic">Save</emphasis>, then save
            the validation rule by clicking <emphasis role="italic">Save</emphasis>.</para>
        <para>To edit a validation rule, click the name the relevant validation rule in the list and
            select &quot;Edit&quot; from the context menu. Then follow the same procedures as above
            when creating a new rule.</para>
        <para>To delete a validation rule, click the name of the validation rule to be deleted, and
            select &quot;Remove&quot; from the context menu.</para>
        <para>Validation rule names can be translated to other languages by selecting the name of
            the validation rule and selecting &quot;Translate&quot; from the context menu. </para>
        <para>To view validation rule details, click the name of the validation rule, and then
            select &quot;Show details&quot; from the context menu. </para>
    </section>
    </section>
